# 性能优化：第一项交付与远端验收

当前实现仅覆盖第一项测量与回归工具。后续 Run 隔离、预检、NPZ、目标转换、DIoU、D2H、SoftNMS、指标缓存及 Loader 调优，须在本项真实验收通过后逐项实施。正常训练不启用 profiler 或数值证据采集。

## 原机器准备

在仓库根目录执行。使用原训练环境，不升级依赖、不设置新的线程数或精度开关。`原始配置.json` 可以是原始配置对象，也可以是含 `config` 字段的训练结果 JSON。保留原始 epoch 数、seed 和所有训练配置；基线 BS 必须为 64，核对 workers=8、prefetch=4。

先提供 E13 checkpoint、对应配置、原始未舍入指标，并确认指标是 val 还是 test。报告中的参考展示值不能证明逐浮点一致。本地只有参考产物时，不把它当成原 E13。

以下命令中的中文路径是需要替换的实际输入；输出目录必须不存在。建议输出到仓库外，或者被忽略的 `ckpt/性能验收/`。每条 `run` 命令启动独立进程，不在同一模型上切换开关。仅加载可信的本地 checkpoint 和证据文件。

## 1. 静态及本地测试

```text
python -m py_compile src/perf_monitor.py src/perf_regression.py src/training.py src/eval.py scripts/性能回归.py tests/测试性能回归.py
python -m pytest tests/测试性能回归.py -q
python -c "import src.training; import src.eval"
python -m scripts.性能回归 --help
```

测试包含 CPU/CUDA 轻量模型真实训练循环、10 步状态、梯度、原始预测、SoftNMS/AP/AR 的 profiler 开关严格比较，以及采样边界、NPY 优先级、两轮 sampler 顺序和 RNG 对照。训练循环测试从 AST 加载实际循环，使用轻量模型，不代表真实 AuViRe 模型依赖已经可导入。CUDA 不可用时对应测试会跳过。

## 2. 数值采集：完整训练与 Test

```text
python -m scripts.性能回归 run --config 原始配置.json --mode capture --output ckpt/性能验收/关闭采集
python -m scripts.性能回归 run --config 原始配置.json --mode capture --profile --output ckpt/性能验收/开启采集
python -m scripts.性能回归 compare --baseline ckpt/性能验收/关闭采集 --candidate ckpt/性能验收/开启采集
```

默认运行配置规定的完整训练或原 patience 早停，然后加载该次最佳模型进行 Test。显式 `--epochs 4` 仅适合排错，不等同完整回归。默认使用配置第一个 seed；多 seed 配置需逐个用 `--seed` 分别运行。

采集保存首 1000 train、500 val、500 test 输入、全部 batch ID 顺序、前 10 步 forward/loss/梯度/model/optimizer/scheduler/RNG、逐 epoch 指标和 scheduler，以及每轮验证和最终 Test 的原始预测、GT、proposals、未舍入指标。证据未压缩，完整训练需为每轮预测及状态文件预留磁盘空间。保留 `None` 梯度，不转换为零。

工具只消费实际训练/评估迭代，不额外遍历 sampler。实际 NPZ/NPY 来源与文件 SHA256 在计时前采集；源代码内容摘要同时记录未提交修改。采集遇到原 loader 返回 `None` 时直接报错，不将丢样本结果作为基线；这不改变正常训练的数据错误处理。

采集本身包含大量 D2H 和写盘，**其耗时不能用作性能验收**。比较成功只证明所保存证据一致；报告单独显示完整训练覆盖是否满足。还需核对原分支基线，确认新增观测逻辑本身未改变结果。

## 3. 固定 E13 评估

确认指标属于验证集时执行以下命令；若属于 Test，两个命令都替换为 `--reference-split test`。工具保持训练内评估协议，不自动套用 `scripts/test.py` 的 generalization 或 MHA fastpath 设置。

```text
python -m scripts.性能回归 run --config 原始配置.json --mode eval --checkpoint E13.pth --reference-split val --output ckpt/性能验收/E13关闭
python -m scripts.性能回归 run --config 原始配置.json --mode eval --checkpoint E13.pth --reference-split val --profile --output ckpt/性能验收/E13开启
python -m scripts.性能回归 compare --baseline ckpt/性能验收/E13关闭 --candidate ckpt/性能验收/E13开启
```

若原 E13 使用不同评估入口，先明确并恢复原评估协议，不修改此工具来追逐展示值。固定评估一致不代表完整训练已复现。

## 4. Profiler 开销：三组独立配对

分别以 `关闭1/开启1`、`关闭2/开启2`、`关闭3/开启3` 为输出目录运行下面两条命令。第二组先运行开启，再运行关闭，减小固定运行顺序带来的偏差。避免同机并行训练。

```text
python -m scripts.性能回归 run --config 原始配置.json --mode benchmark --epochs 4 --output ckpt/性能验收/关闭1
python -m scripts.性能回归 run --config 原始配置.json --mode benchmark --epochs 4 --profile --output ckpt/性能验收/开启1
```

```text
python -m scripts.性能回归 timings --profiler-overhead --baselines ckpt/性能验收/关闭1 ckpt/性能验收/关闭2 ckpt/性能验收/关闭3 --candidates ckpt/性能验收/开启1 ckpt/性能验收/开启2 ckpt/性能验收/开启3
```

每次首个完整 epoch 为 warmup，后三轮统计 mean/median。三组开销均须低于 2%。工具核验配置、环境、依赖、数据清单、batch 顺序和 epoch 数值指标；只输出计时门槛结果，不自动接受 commit。波动过大时重测，不把负开销解释为加速。

每 100 个 batch 采样第 100、200……个 batch；不足 100 的短运行没有训练采样，可在排错时指定 `--interval 1`，但正式开销验收保持 100。GPU 阶段使用当前设备 stream 的 CUDA Event；所有阶段结束后在采样窗口边界统一等待。CPU 时间包含主机等待；GPU 时间是 stream 区间，并非纯 kernel 时间。阶段耗时不能求和代替 epoch wall time。

epoch wall time 覆盖训练、验证、指标和 scheduler，不含随后保存 checkpoint 和报告；当前阶段不能据此宣称 checkpoint/I/O 已提速。benchmark 不保存数值 tensor，不执行最终 Test，也不以早停缩短四轮实验；scheduler 仍采用原配置的训练长度。

GPU util、CPU、RAM、I/O wait 使用原机器外部采样工具，另存同一时间段日志。例如 NVIDIA 环境可运行 `nvidia-smi --query-gpu=timestamp,index,utilization.gpu,memory.used --format=csv -l 1`，Linux 可用已有的 `pidstat -u -r -d -p <训练PID> 1` 与 `vmstat 1`。将资源采样方式在配对运行中保持一致；未采集字段在报告中为 null，不当作零或已通过。

## 回传与停点

回传运行元数据、依赖版本、验收报告、阶段耗时、三组计时比较结果、数值比较结果及资源日志；完整 tensor 证据保留在原机器，比较失败时回传首个差异路径。

通过要求：原始基线可重复、输入/顺序/数值一致、E13 原始指标一致、完整训练与 Test 一致、profiler 开销低于 2%。任何一项未完成，本项保持“待验收”，不进入下一项，也不默认采用新的 Loader 参数。
