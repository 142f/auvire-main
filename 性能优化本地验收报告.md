# 第一项：测量与回归工具

状态：本地验证通过，原训练机器验收待完成。后续优化未实施。

修改前代码：`86a9973637d9ed05b3b7b920e3b2b0bbd1036654`。

## 已通过

- 新增及修改 Python 文件的 `py_compile`，独立 CLI `--help`，`git diff --check`。
- `python -m pytest tests/测试性能回归.py -q`：13 passed。
- CPU 与 CUDA 轻量模型：真实训练循环的 profiler 开关对照，前 10 步预测、loss、逐参数梯度、模型、optimizer、scheduler、RNG 严格一致；验证原始预测、proposals 和 AP/AR 严格一致。
- 两轮普通 DataLoader 与 ID 观测包装的 batch 内容及 generator 状态一致；含 Windows spawn、2 workers、persistent workers、prefetch=4 和最后不足 batch。
- CUDA Event 只在采样窗口边界等待；关闭 profiler 不创建 CUDA Event。
- 数值比较拒绝 dtype、结构、微小数值和 None/零梯度差异；计时比较拒绝超标开销、重复运行目录及环境变化。
- 独立对照修改前 Git 版本：通过 AST 加载修改前训练循环及完整 Evaluation，与新实现运行同一轻量模型，CPU/CUDA 的 epoch 指标、模型参数、optimizer 状态全部严格一致。

Windows 多进程测试最初因沙箱拒绝创建命名管道失败，随后在沙箱外重跑通过。另补充失败报告及禁止覆盖已有输出的测试。

## 尚未验收

- 本地缺少 `transformers`，`import src.training` 失败；未通过伪造依赖或替代真实模型来声称导入成功。轻量循环测试不依赖该模型后端。
- 未取得原训练机器完整数据和已确认归属的 E13 产物，因此尚未执行真实模型的 1000/500/500 输入比较、10 步回归、固定 E13 评估和完整训练复现。
- 尚无三组真实 profiler 开关性能报告，不声称开销低于 2% 或任何训练提速。
- GPU util、CPU、RAM、I/O wait 日志需按远端说明采集；内置报告缺失值为 null。

下一步按《性能优化验收说明.md》在原环境执行并回传报告。本项通过全部门槛后，再实施 Run/checkpoint 隔离。
